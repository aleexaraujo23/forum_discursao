<p>
 <div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title"> <%= @discussion.headline %> </span>
           <div class="card-action">
            <p><%= @discussion.content %></p>
            <br>
            <% if @discussion.url_image? %>
             <img <%= image_tag @discussion.url_image, width:100, heidth:200 %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="left"><%= @discussion.comments.count %>   Comentário(s) </div>
  <br>
  
  <%= render @discussion.comments %> 

  <h4> Responder ao Tópico </h4>
  
  <%= render 'comments/form' %>


<%= link_to 'Editar', edit_discussion_path(@discussion), class: "waves-effect waves-teal btn-flat" %> |
<%= link_to 'Home', discussions_path, class: "waves-effect waves-teal btn-flat" %>
